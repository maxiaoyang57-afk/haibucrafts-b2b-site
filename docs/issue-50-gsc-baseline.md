# Issue #50 GSC and discovery baseline

Baseline source: `origin/main` at `219672d6ca85d5d2d51e5ceabfc3854ca07c0eaf` (2026-09-09). This is a code and HTTP baseline, not a GSC export.

## Verified in repository

- Catalog: 112 products across four categories.
- Seasonal slime collections: Halloween 6 cards; Christmas 7 cards including priority SLM26529.
- Robots policy: production allows general crawlers plus OAI-SearchBot and ChatGPT-User, and disallows `/api/` and `/v2-preview/`.
- Sitemap: generated from the production route map; seasonal pages and selected product detail routes are included when indexable.
- Production URL policy: existing canonical, redirects and category paths are preserved; this issue does not introduce `/themes/halloween/` or `/themes/christmas/`.

## GSC access status

GSC was not verified in this execution. Do not use `site:` result counts as an indexing metric. Before release review, export or record Search Console data for:

- valid indexed seasonal pages and exclusions;
- non-brand impressions/clicks for Halloween and Christmas queries;
- US, UK, CA, DE and FR source performance;
- inquiry submission, qualified inquiry, quote, sample and order outcomes.

Google AI traffic is included in the Web performance view and cannot be isolated reliably from every AI surface. External AI referrals should be tracked with referrer/UTM data; missing attribution is unknown, not zero.

## Commercial fields requiring confirmation

Confirm target arrival dates, selling-unit quantity/weight, assortment ratios, MOQ, stock status, lead time, approved substitutions, packaging artwork, destination warnings and any sample or testing scope during quotation.
